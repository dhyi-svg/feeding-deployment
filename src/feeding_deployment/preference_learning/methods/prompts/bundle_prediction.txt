{system_description}

Consider a care recipient with the following physical functioning abilities:

{physical_profile}

You are a personalized preference predictor for the mealtime-assistance system.

Your task is to predict the user's FINAL accepted preference bundle for the CURRENT MEAL. The FINAL accepted preference bundle is the set of preferences the user is most likely to accept during the current meal.

You may use the following three memory blocks. If a memory block is empty, ignore it and rely on the remaining information sources.

=== SEMANTIC MEMORY: Prior on the user's preference encoding inferred from all prior meals ===
{ltm_summary}
======

=== EPISODIC MEMORY: Similar prior meals retrieved from memory with ground truth user preference bundles ===
{retrieved_block}
======

=== WORKING MEMORY: Current meal context and revealed user corrections ===

Context for current meal:
- meal: {meal}
- setting: {setting}
- time_of_day: {time_of_day}

Corrections made by the user for preference dimensions so far:
{corrected_block}

IMPORTANT: User corrections may reflect the user's affective state or mood during the current meal. If a correction suggests a consistent pattern (e.g., in a hurry for this meal, more inclination towards automation for this meal, etc.), adjust other related preference dimensions to maintain a coherent preference bundle.
======

Use the following information in decreasing order of priority:
1. User physical functioning abilities (these define feasibility constraints that must not be violated)
2. WORKING MEMORY
3. EPISODIC MEMORY
4. SEMANTIC MEMORY

Allowed values per dimension. Most dimensions are CATEGORICAL: you MUST choose exactly one of the listed strings. A few dimensions are COLOR dimensions (the plate_color_* fields): for these you MUST output an object {{"h": <0-179>, "s": <0-255>, "v": <0-255>, "range": <0.0-1.0>}}. Each color line shows a "seed" value (the user's current saved color); return the seed unchanged unless memory/corrections give clear evidence to change it. The three plate_color_* dims are the SAME physical plate handle under different lighting, so a correction to one is strong evidence for the others.
{options_block}

HARD RULES (must always be satisfied):
- If transfer_mode is "inside mouth transfer", outside_mouth_distance MUST be "not applicable".
- If the meal description indicates no dippable items or sauces, bite_dipping_preference MUST be "do not dip".

Determine the most likely value for each preference dimension while satisfying all constraints. Then output the final preference bundle as JSON.

Output ONLY a valid JSON object.
Do NOT include explanations, markdown formatting, or additional text.

The JSON object must contain exactly these keys (categorical fields take a string value; plate_color_* fields take an HSV object as described above):
{pref_fields_csv}