<launch>
  <!-- LEGACY standalone bringup: vention.urdf still assumes ZED-owned odom
       (inverted joint). NEVER run alongside sensors.launch (two-parent TF). -->
  <param name="robot_description"
         textfile="$(find feeding_deployment)/urdf/vention.urdf" />

  <!-- publishes /joint_states (headless) -->
  <node pkg="joint_state_publisher"
        type="joint_state_publisher"
        name="joint_state_publisher" />

  <node pkg="robot_state_publisher"
        type="robot_state_publisher"
        name="robot_state_publisher" />
</launch>
